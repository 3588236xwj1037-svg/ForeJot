# 贡献指南

感谢你帮助改进驻笺。

## 开发流程

1. Fork 仓库并从 `main` 创建分支。
2. 使用 `npm ci` 安装锁定版本的依赖。
3. 保持改动聚焦，并为行为变化补充测试。
4. 提交前运行 `npm test` 和 `npm run build`。
5. 创建 Pull Request，说明问题、解决方式和验证结果。

## 隐私要求

不要提交真实便签、`notes.json`、应用数据目录、截图中的私人内容、密钥或本机绝对路径。复现问题时请使用最小化的虚构数据。

## 问题报告

请包含驻笺版本、Windows 版本、复现步骤、预期结果和实际结果。涉及安全或隐私的问题不要创建公开 Issue，请使用 `SECURITY.md` 中的私密报告方式。
