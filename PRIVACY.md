# 驻笺隐私说明

最后更新：2026-08-11

驻笺是一款由独立开发者维护、本地优先的 Windows 桌面便签软件。本文说明软件处理数据的方式。

## 收集的数据

驻笺不要求注册账号，不包含广告、用户行为分析、崩溃遥测或云同步，也不会把便签内容上传给开发者。

便签正文、标题、内嵌图片和应用设置保存在当前 Windows 用户的本地文件中：

```text
%APPDATA%\floating-notes\notes.json
```

这些数据默认不会离开用户的电脑。

## 网络行为

驻笺本身不提供云服务，但以下由用户内容或操作触发的功能可能访问网络：

- 预览含 `http://` 或 `https://` 图片地址的 Markdown 时，图片请求会发送到对应图片服务器。
- 导入引用远程图片的 Markdown 时，驻笺会尝试从原地址下载受支持的图片并保存为便签内嵌附件。
- 点击外部链接时，驻笺会把 HTTP 或 HTTPS 地址交给系统默认浏览器打开。
- 下载应用更新、访问 GitHub 或浏览器本身产生的网络行为不由驻笺处理。

远程服务器可能按其自身政策记录 IP 地址、请求时间、User-Agent 和目标 URL。不要在不信任的 Markdown 中打开敏感的远程资源。

## 本地权限

驻笺仅在相关功能需要时使用：

- 用户主动选择的 Markdown、图片和导出路径。
- 剪贴板中的文字或图片，用于粘贴到便签。
- Windows 开机启动设置，仅在用户启用该选项时写入。
- 应用自己的 `%APPDATA%` 数据目录。

## 保存、备份与删除

数据会一直保留在本地，直到用户删除便签或删除 `notes.json`。卸载应用后，Windows 可能保留用户数据目录，便于重新安装后继续使用。

备份时可复制 `notes.json`。恢复前应完全退出驻笺，并先备份目标电脑上已有的数据；覆盖该文件会替换现有便签状态。

## 第三方组件

驻笺使用 Electron、React、KaTeX 等开源组件在本地运行。它们的许可证和源码依赖信息可在本仓库及 `package-lock.json` 中查看。

## 隐私说明更新

如果软件未来增加云同步、自动更新、遥测或其他会改变数据处理方式的功能，应在发布前更新本文，并在版本说明中明确告知。

## 联系方式

仓库公开后，可通过 GitHub Issue 提交一般隐私问题。涉及漏洞、私人便签或其他敏感信息时，请按 [SECURITY.md](SECURITY.md) 使用 GitHub Security Advisory 私密报告，不要发布到公开 Issue。
